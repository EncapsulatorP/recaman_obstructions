
### 3. recaman_obstructions
**Core opportunity:** Turn the obstruction classification into the **One explicit density formula** for Recamán gaps.

**Action plan (4 weeks):**
1. **Week 1:** Extend the obstruction catalog to 10^6 terms. Fit a precise power-law + oscillatory model for the frequency of each obstruction class.
2. **Week 2:** Derive a closed-form **singular series analogue** for Recamán (using the same mod-30 framework you already have). Prove it matches the observed frequencies to 4 decimal places.
3. **Week 3:** Conjecture and verify (up to 10^7) a new Recamán density law: “The probability of obstruction class k at step n is exactly C_k / log n + O(1/n).”

#### Corrections
- Separate process-side obstruction density from value-side hole density everywhere in the writeup and scripts. They are related but not interchangeable.
- Audit any claims of "exact density formula" versus "empirical fit" and downgrade wording where the evidence is still computational.

- Add a correction note on checkpoint interpretation: early-step behavior is transient, while large-step behavior is closer to the asymptotic regime.

#### Improvements
- Extend `scripts/densities.py` with CSV export for band densities, so local dense/sparse windows can be compared across checkpoints.
- Add side-by-side summaries for global fill density, local band density, obstruction density, and point-hit density.
- Add a comparison workflow against known external benchmarks, especially the smallest missing values and high-span hole structure reported by Chaffin.
- Add plots for moving-window density, cumulative hole persistence, and "dense zone -> sparse zone -> dense zone" transitions.
- Add a dedicated subsection on uncertainty: which observed patterns are stable across scales and which may still be finite-range artifacts.

#### New Paths

##### Complex Plane Generalization
- Replace the 1D visited set with Gaussian integer or Eisenstein integer lattices.
- Define backward/forward moves by a norm or argument rule.
- Test whether obstruction bands survive in 2D arithmetic space.

##### Dynamic Generalization With Fixed Points
- Introduce maps where the step rule includes attracting, neutral, or repelling fixed points.
- Test whether Recamán-like obstruction alternation persists when local return dynamics are built in.

##### 3D Topological Generalizations
- Lift the trajectory into 3D state spaces using delay embeddings, knot-like arc traces, or simplicial complexes on visited points.
- Measure whether persistent homology or winding invariants capture obstruction clusters better than raw density.

##### Integrations
- Connect the current density work to `recaman_phase_space_3d.py`, `recaman_wheel_validator.py`, and the value-side obstruction pipelines.
- Produce one report that compares process-side and value-side structure directly.

##### Cross-Model Generalization
- Build a small family of Recamán variants with modified legality rules, modulo wrapping, or weighted jumps.
- Compare which density features are universal and which are rule-specific.
